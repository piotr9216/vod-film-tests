[tool:pytest]
testpaths = tests
addopts = 
    --verbose
    --strict-markers
    --strict-config
    --html=reports/test-report.html
    --self-contained-html
    --browser chromium --headed
markers =
    e2e: End-to-end tests
    api: API tests
        slow: slow running tests